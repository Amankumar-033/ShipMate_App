export const BASE_RATE_PER_KM_PER_KG = 0.05; // base rate in default currency (e.g., $0.05 per km per kg)
export const EXPRESS_MULTIPLIER = 1.5; //shipping cost multiplier for express  shipping
export const STANDARD_MULTIPLIER = 1.0;
export const DEFAULT_CURRENCY = 'USD';

//This file contains constants used throughout the application, such as base rates and multipliers for shipping costs.